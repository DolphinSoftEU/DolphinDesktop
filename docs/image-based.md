# Image-Based Selectors

This page has moved to [Guides: Image-Based Fallback](guides/image-based.md).

Use image matching as a last resort when UIA or Win32 selectors cannot see a rendered control. Install the optional vision dependencies first:

```bash
pip install "dolphin-desktop[vision]"
```
